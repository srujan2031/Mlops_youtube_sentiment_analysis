from setuptools import find_packages, setup

setup(
    name='mlops_youtube_sentiment',  # ← your project name
    version='0.0.0',  # ← bump version if needed
    author='Srujan Chinta',  # ← your name
    author_email='chintasrujan07@gmail.com',  # ← your email
    packages=find_packages(),
    install_requires=[
    ]
)
